apiVersion: v1
kind: Service
metadata:
  name: lunch-api
  namespace: lunch
spec:
  type: NodePort
  selector:
    app: lunch-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080

